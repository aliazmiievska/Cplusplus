

functions (lec_01):

{

    max()


    hypot (x, y) 
    Повертає sqrt (x2 + y2)

    fma (x, y, z) =     x * y + z 

    log(x)
    натуральний логарифм від х

    log10(x) десятковий логарифм числа х

    log2(x)

    sqrt(x) = x²
    cbrt(x) = x³
    pow (x, y) Повертає значення x у ступінь y 

    round(), - математичне округлення
    ceil(), - округлення до більшого
    floor() - округлення в сторону меншого
    trunc() – відкидання дробової частини   
}


int * float = float (ширша область значень)